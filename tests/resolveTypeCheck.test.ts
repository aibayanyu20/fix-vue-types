import { describe, expect, it } from 'vitest'
import { resolveTypeElements } from '../src/resolveType'

describe('resolveType check', () => {
  it('import resolveType', () => {
    expect(resolveTypeElements).toBeDefined()
  })
})
